apply plugin: "war"

bootWar {
    mainClassName = "com.terabiz.main.MainApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
